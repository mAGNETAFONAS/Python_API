apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: web-helm
  labels:
      app: web
      release: prometheus
spec:
  selector:
    matchLabels:
      app: web
  endpoints:
  - port: http
    interval: 15s
    path: /metrics
